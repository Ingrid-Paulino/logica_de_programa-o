// Escreva em pseudocódigo, as seguintes expressões matemáticas:
/*
  a) (A + B)*C
  b) TOTAL / N
  c) A - B(C + D 2 ) / E
  d) base expoente
  e ) a * b c
*/
//  ---------------------------------RESULTS------------------------------------------

// a)
  /*
    inicio
    var A, B, C, RESULT
    ler(A)
    ler(B)
    ler(C)
    RESULT <- (A + B)*C
    escrever(RESULT)
    fim
  */

// b)
  /*
    inicio
    var TOTAL, N, RESULT
    ler(TOTAL)
    ler(N)
    RESULT <- TOTAL/N
    escrever(RESULT)
    fim
  */

// c)
  /*
    inicio
    var A, B, C, D, E, 2, RESULT
    ler(A)
    ler(B)
    ler(C)
    ler(D)
    ler(E)
    ler(2)
    RESULT <- A - B(C + D^2)/E
    escrever(RESULT)
    fim
  */

// d)
  /*
    inicio
    var BASE, EXPONETE
    ler(BASE)
    ler(EXPONETE)
    RESULT <- BASE^EXPOENTE
    escrever(RESULT)
    fim
  */

// e)
  /*
    inicio
    var A, B, C
    ler(A)
    ler(B)
    ler(C)
    RESULT <- A * B^C
    escrever(RESULT)
    fim
  */